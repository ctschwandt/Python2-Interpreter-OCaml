# Python2 Interpreter (OCaml)

A Python-like interpreter implemented in OCaml.  
This project builds a subset of Python with support for functions, control flow, data structures, and an interactive REPL.
